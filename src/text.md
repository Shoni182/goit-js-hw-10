🟦 1. Підготовка файлів У файлі 1-timer.html

Створи базову HTML-структуру.

Додай:

інпут для вибору дати (id="datetime-picker"),

кнопку запуску (data-start),

блок таймера зі span-елементами для днів, годин, хвилин, секунд (із відповідними
data-атрибутами).

Додай класи для стилізації згідно макету.

У файлі 1-timer.js

Імпортуй:

бібліотеку flatpickr,

її CSS,

бібліотеку iziToast,

її CSS.

🟦 2. Ініціалізація елементів

У 1-timer.js:

Отримай:

інпут #datetime-picker,

кнопку [data-start],

елементи, що показують значення:

[data-days]

[data-hours]

[data-minutes]

[data-seconds]

Зроби кнопку Start неактивною при завантаженні сторінки.

🟦 3. Змінні для роботи

Створи змінну let userSelectedDate = null;

Створи змінну let timerId = null; для інтервалу.

🟦 4. Налаштування flatpickr

Створи об’єкт options, в який увійдуть:

enableTime: true

time_24hr: true

defaultDate: new Date()

minuteIncrement: 1

метод onClose(selectedDates).

Передай options у flatpickr.

🟦 5. Реалізуй onClose()

У методі onClose:

Отримай дату з selectedDates[0].

Перевір:

якщо вона менша за поточну →

показати повідомлення через iziToast,

кнопка Start → неактивна,

НЕ зберігати дату.

якщо дата більше поточної →

зберегти в userSelectedDate,

активувати кнопку Start.

Якщо після вибору валідної дати користувач вибере невалідну — кнопка знову
неактивна.

🟦 6. Обробка кнопки Start

При натисканні на кнопку Start:

Зроби кнопку Start — неактивною.

Заблокуйте інпут (disabled = true).

Запусти setInterval раз на секунду.

В інтервалі:

обчисли різницю між userSelectedDate і поточним часом.

якщо різниця ≤ 0:

зупини інтервал,

виведи 00:00:00:00,

розблокуй інпут,

кнопка Start залишається неактивною,

завершити роботу.

конвертуй мілісекунди функцією convertMs().

відформатуй значення через addLeadingZero().

онови інтерфейс.

🟦 7. Функція convertMs

Додай готову функцію convertMs (вона вже дана).

Використовуй її для розрахунку часу.

🟦 8. Функція addLeadingZero

Створи функцію addLeadingZero(value):

Перетворюй value у рядок.

Використовуй padStart(2, "0").

Поверни відформатоване значення.

🟦 9. Оновлення інтерфейсу таймера

У спеціальній функції:

Запиши отримані значення в DOM:

data-days

data-hours

data-minutes

data-seconds

Дні можуть бути більше ніж 2 цифри — не форматуємо.

Години/хвилини/секунди обов’язково форматуємо.

🟦 10. Зупинка таймера

Коли залишок часу = 0:

Зупинити інтервал.

Встановити всі значення в "00".

Розблокувати інпут.

Кнопка Start залишається заблокованою.

🟦 11. Перевірка відповідності критеріям

Після завершення реалізації переконайся:

✔ flatpickr і iziToast підключені ✔ При завантаженні кнопка Start не активна ✔
Календар відкривається ✔ Дата в минулому → повідомлення → кнопка неактивна ✔
Дата в майбутньому → кнопка активна ✔ Після Start:

кнопка й інпут блокуються

відлік працює щосекунди ✔ Таймер зупиняється в кінці ✔ Показує 00:00:00:00 ✔ Час
відформатований addLeadingZero

Хочеш — можу скласти ще й чек-лист, або написати готовий шаблон коду, або
покроково провести через реалізацію з поясненнями.
